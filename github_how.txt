echo "# flask_login" >> README.md

git init ==위 두 줄은 새로운 repository만들 때 실행

git add README.md ===새로운 파일 생성때 실행 ==='' add . '' 은 all 의미

git commit -m "first commit" ===" "안은 주석내용

git branch -M main ===repository의 자식

git remote add origin https://github.com/sukachoi/flask_login.git

git push -u origin main ===db에서 commit과 같은 의미 ==push이후 생략가능

git pull ==수정파일 받아보기